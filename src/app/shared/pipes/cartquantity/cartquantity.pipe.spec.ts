import { CartquantityPipe } from './cartquantity.pipe';

describe('CartquantityPipe', () => {
  it('create an instance', () => {
    const pipe = new CartquantityPipe();
    expect(pipe).toBeTruthy();
  });
});
